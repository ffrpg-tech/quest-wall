node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Offline data-fetch tooling and its output — not committed; regenerated
# locally and injected via the deploy platform's build-time env vars instead.
/api
/static
/scripts

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results
